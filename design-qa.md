<!-- 자격증봄 네이티브 적응형 화면과 Android 릴리스 최적화의 디자인 QA 상태를 기록한다. -->

# 자격증봄 0.9.0 적응형 디자인 QA

## 비교 기준

- 기존 자격증봄의 인디고·크림 디자인 토큰, 패밀리 워드마크, 화면별 카드와 하단 메뉴를 시각 정본으로 유지했다.
- 이번 요청에는 자격증봄 전용 목표 스크린샷이 없으므로 새로운 색상·폰트·카드 스타일을 만들지 않았다.
- 구현 대상 폭은 320, 390, 430, 600, 720, 768, 900, 1024, 1440px이며 시스템 글자 크기 100%, 135%, 200%를 포함한다.

## 구현 결과

- 320~599px 휴대전화는 한 열, 600~899px 폴더블·소형 태블릿은 두 열, 900px 이상 태블릿은 넓은 중앙 콘텐츠를 사용한다.
- 시스템 큰 글자에서는 넓은 화면도 목록과 통계가 한 열로 돌아가 숫자·날짜·버튼이 잘리지 않게 했다.
- 홈, 시험 찾기, 달력, 내 일정, 설정, 상세 화면의 콘텐츠 폭과 좌우 여백을 같은 적응형 규칙으로 통일했다.
- 알림 편집기는 휴대전화에서 하단 시트, 폴더블·태블릿에서 최대 폭을 제한한 중앙 모달로 표시한다.
- 하단 메뉴는 화면 전체 폭을 무작정 늘리지 않고 태블릿에서 중앙 정렬한 최대 폭을 사용한다.

## 검증

- 적응형 계산 단위 테스트는 320px, 390px, 720px, 1024px, 768px·200% 글자를 검증한다.
- 모바일 Android·iOS export, 타입 검사, 설정 검사와 전체 저장소 테스트를 통과했다.
- 웹 Playwright는 desktop, small-mobile, mobile, webkit-mobile에서 109개 통과·3개 의도적 제외를 기록했고 320px·200% 글자 가로 넘침 회귀를 확인했다.
- Android prebuild에서 화면 회전 제한 없음, API 36, edge-to-edge, R8 코드 축소와 리소스 축소 설정을 확인했다.

## 남은 확인

- 현재 실행 환경에 연결된 Android/iOS 실기기나 시뮬레이터가 없어 네이티브 화면을 직접 캡처해 기존 화면과 같은 캔버스에서 비교하는 단계는 `BLOCKED_DEVICE`다.

final result: blocked_device
